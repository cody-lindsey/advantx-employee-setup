# Architecture

This repo is intentionally simple:

- `index.html` is the single-page onboarding guide.
- Image assets live at the repo root.
- `scripts/verify.sh` enforces basic hygiene in CI.

Non-goal: this repo is not a full website or application.

